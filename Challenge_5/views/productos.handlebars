{{#if productos_list }}
<table class="table">
        <thead>
          <tr>
            <th scope="col">ID</th>
            <th scope="col">Título</th>
            <th scope="col">Precio</th>
            <th scope="col">Imagen</th>
          </tr>
        </thead>
        <tbody>
          {{#each productos_list}}
              <tr>
                  <th scope="row">{{Id}}</th>
                  <td>{{title}}</td>
                  <td>{{price}}</td>
                  <td><img src="{{thumbnail}}" alt="{{title}}" width=50 height=50></td>
                </tr>
          {{/each}}
        </tbody>
      </table>
  {{else}}
    <h5 class="alert-danger rounded p-2">No hay productos</h5>  
{{/if}}
